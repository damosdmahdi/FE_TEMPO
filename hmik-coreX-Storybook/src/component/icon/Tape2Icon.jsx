import Icon from "../../assets/tape2.svg";

const Tape2Icon = ({
  width = 70,
  height = 70,
  alt = "Icon Tape 2",
}) => {
  return (
    <img
      src={Icon}
      width={width}
      height={height}
      alt={alt}
    />
  );
};

export default Tape2Icon;